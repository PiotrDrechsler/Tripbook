---
import Layout from "@/layouts/Layout.astro";
import RegisterForm from "@/components/auth/RegisterForm";

// TODO: Check if user is already logged in and redirect to /trips
// This will be implemented when backend auth is ready
---

<Layout title="Rejestracja - Tripbook">
  <div class="min-h-screen flex items-center justify-center bg-gradient-to-br from-indigo-50 via-white to-purple-50">
    <RegisterForm client:load />
  </div>
</Layout>
